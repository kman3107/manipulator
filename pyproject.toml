[project]
name = "manipulator"
version = "0.1.0"
description = "Bulk filename editor with regex filtering (Tkinter GUI)."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "sv-ttk>=2.6.1",
    "tkinterdnd2>=0.4.3",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
]
